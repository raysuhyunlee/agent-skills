interface:
  display_name: "Store Listing"
  short_description: "ASO-backed store metadata with approval gates"
  default_prompt: "Use $ray-store-listing to create ASO-backed store metadata for my app."
